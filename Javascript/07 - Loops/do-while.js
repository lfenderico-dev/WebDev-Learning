"use strict";

// Same as while, but the condition is checked at the end, not at the start.

// Syntax:
/*
    do {
        code
    } while (condition);
*/

// Example:
let x = 0;

do {
    alert(x);
    x++;
} while (i < 5);
