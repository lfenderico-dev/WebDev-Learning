"use strict";

let number = 10; // Int number
let number2 = 99.99; // Float number
let a = infinity; // Infinity is also a special number
let b = -infinity; // Minus infinity is also a special number.
let c = NaN; // Means not a number, so an error.

    // With numbers you can do operations:
    let sum = 10 + 10; 
    let subtraction = 10 - 10;
    let multiplication = 10 * 10;
    let quotient = 10 / 10;
    let reminder = 10 % 10;
    let exponentiation = 10 ** 2;


    // Increment and decrement:
    let n1 = 5;

    ++n1 // 6
    --n1 // 4


    // Manipulate variable value:
    let n = 2;

    n += 5; // Now n = 7
    n -= 2; // Now n = 0
    n *= 10; // Now n = 20
    n /= 2; // Now n = 1
    n %= 2; // Now n = 0
    n **= 3; // Now n = 8



// To check the data type:
typeof number2; //"number"


