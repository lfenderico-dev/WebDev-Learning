"use strict";

// Arrow function: another way to declare a function
let quotient = (a, b) => a / b

console.log(quotient(10, 2)); // Returns 5

// declaration + VariableName + parameters + function body.