"use strict";

// This operator checks if a value is defined, returning the defined one only.

let user;

alert(user ?? "Anonymous");
// In this case "Anonymous" would be displayed because user is not defined.