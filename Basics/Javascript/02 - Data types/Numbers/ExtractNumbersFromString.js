"use strict";

// Use cases: if you want to extract numbers from text or if you have like values in units.

// Example:
alert ( parseInt( `120px` ) ); // 120
alert ( parseFloat ( `3.5vw` ) ); // 3.5

// Use parseInt() for int values.
// Use parseFloat() for floating-point values.

// IMPORTANT: both return NaN if the first character is not a number.