"use strict";

// Array lenght: it is the number of elements in there
console.log( colors.length ); // 3


// Decrese the lenght of an array = truncate the elements
colors.lenght = 2; // truncate one element
console.log(colors); // [`red`, `blue`]


// Clearing an array:
colors.lenght = 0;
console.log(colors); // []
