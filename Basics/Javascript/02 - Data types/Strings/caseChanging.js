"use strict";

let message = `Hello world`;

// To get all lowercase:
alert( message.toLowerCase() );  // hello world

// To get all uppercase:
alert( message.toUpperCase() ); // HELLO WORLD

// To target a single character:
alert( message[0].toLowerCase() ); // hello world